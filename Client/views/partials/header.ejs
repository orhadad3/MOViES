<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet"><% if (userType === "admin" && activePage === 'admin') { %>
    <link href="/css/admin-panel.css" rel="stylesheet"><% } %>
    <title><%= title %></title>
</head>
<body>
    <div class="container-fluid bg-dark-subtle pt-3 shadow border-bottom header">
        <div class="container">
            <nav class="navbar navbar-expand-lg">
                <div class="logo navbar-brand">
                    <a href="/" class="text-decoration-none text-warning">
                        MOViES
                        <i class="bi bi-film"></i>
                    </a>
                </div>

                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav ms-auto">
                        <li class="nav-item">
                            <a class="btn <%= activePage === 'home' ? 'btn-primary fw-semibold' : 'btn-light' %>" aria-current="page" href="/">Home</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn <%= activePage === 'top-links' ? 'btn-primary fw-semibold' : 'btn-light' %>" href="/top-links">Top Links</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn <%= activePage === 'favorites' ? 'btn-primary fw-semibold' : 'btn-light' %>" href="/favorites">Favorites</a>
                        </li>
                        <li>
                            <div class="dropdown">
                                <button class="btn <%= activePage === 'admin' ? 'btn-primary' : 'btn-light' %> dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    Welcome <strong><%= username %></strong>
                                </button>
                                <ul class="dropdown-menu">
                                    <% if(userType === "admin") { %>
                                        <li>
                                            <a class="dropdown-item <%= activePage === 'admin' ? 'fw-semibold active' : '' %>" href="/admin">Admin Panel</a>
                                        </li>
                                    <% } %>
                                    <li><a class="dropdown-item text-danger" href="/logout">Logout</a></li>
                                </ul>
                            </div>
                        </li>
                    </ul>
                </div>
            </nav>
        </div>
    </div>
