<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" type="image/svg+xml" href="/images/favicon.svg">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <link href="/css/login.css" rel="stylesheet">
    <title>MOViES - Login</title>
</head>
<body class="d-flex align-items-center py-4 bg-body-tertiary">
    <main class="form-signin w-100 m-auto">
        <div class="logo navbar-brand">
            <a href="/" class="text-decoration-none text-warning">
                MOViES
                <i class="bi bi-film"></i>
            </a>
        </div>

        <h1 class="h3 mb-3 fw-normal text-center">Please sign in</h1>

        <% if (typeof error !== 'undefined' && error) { %>
            <p class="text-danger"><%= error %></p>
        <% } %>        

        <form action="/login" method="POST" id="login-form">
            <div class="form-floating">
                <input type="text" name="username" value="<%= formData?.username || '' %>" class="form-control" id="floatingInput" placeholder="Username" autocomplete="username">
                <label for="floatingInput">Username</label>
            </div>
            <div class="form-floating">
                <input type="password" name="password" value="<%= formData?.password || '' %>"  class="form-control" id="floatingPassword" placeholder="Password" autocomplete="current-password">
                <label for="floatingPassword">Password</label>
            </div>
            <button class="btn btn-primary w-100 py-2 mt-3" type="submit">Sign in</button>
            <p class="mt-2 mb-3 text-body-secondary text-center">Don't have an account? <a href="/register">Register now</a></a></p>
        </form>
    </main>
    <% if (typeof successMessage !== 'undefined' && successMessage) { %>
        <script type="module">
            import { showAlert } from "/js/utils.js";
            document.addEventListener('DOMContentLoaded', () => {
                showAlert('<i class="bi bi-check-circle-fill text-success"></i> <%= successMessage %>');
                setTimeout(() => {
                    window.location.href = '/';
                }, 1500);
            });
        </script>
    <% } %>
</body>
</html>